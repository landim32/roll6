meta {
  name: Decline invite
  type: http
  seq: 14
}

post {
  url: {{baseUrl}}/api/campaigncharacter/{{declineId}}/decline
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.status: eq 4
}

docs {
  Only the character owner.
}
