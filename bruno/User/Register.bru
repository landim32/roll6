meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/user
  body: json
  auth: none
}

body:json {
  {
    "name": "Rodrigo",
    "email": "{{email}}",
    "password": "{{password}}"
  }
}

assert {
  res.status: eq 201
}
