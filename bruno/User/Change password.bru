meta {
  name: Change password
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/user/password
  body: json
  auth: inherit
}

body:json {
  {
    "currentPassword": "{{password}}",
    "newPassword": "{{newPassword}}"
  }
}

assert {
  res.status: eq 204
}
