meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/campaign
  body: json
  auth: inherit
}

body:json {
  {
    "name": "A Maldição de Strahd",
    "open": false
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  if (res.status === 200 || res.status === 201) {
    bru.setVar("campaignId", res.body.campaignId);
  }
}
