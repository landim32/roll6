meta {
  name: List
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/campaign?page=1&pageSize=20
  body: none
  auth: inherit
}

params:query {
  page: 1
  pageSize: 20
}

assert {
  res.status: eq 200
}

docs {
  Only the campaigns of the authenticated user.
}
