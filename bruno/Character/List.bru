meta {
  name: List
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/character
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

docs {
  Only the characters of the authenticated user.
}
