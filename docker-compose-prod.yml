# Production environment (ASPNETCORE_ENVIRONMENT=Production).
# SSL is terminated by Caddy (automatic Let's Encrypt) in front of the web container (nginx serving the
# SPA and proxying /api to the API). The API and database are not exposed directly.
# Usage: docker compose --env-file .env.prod -f docker-compose-prod.yml up --build -d
# Fixed project name: containers, network and volumes are prefixed "roll6" whatever the folder is called.
name: roll6

services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: roll6-api
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Roll6Context: ${CONNECTION_STRING}
      Jwt__Secret: ${JWT_SECRET}
      S3__Folder: ${S3_FOLDER:-roll6}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - roll6-network
      - roll6-external

  db:
    image: postgres:17-alpine
    container_name: roll6-db
    restart: unless-stopped
    # No port exposed externally — only reachable from the internal network.
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - roll6-network

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ""
    container_name: roll6-web
    restart: unless-stopped
    # Not exposed: only Caddy reaches it.
    depends_on:
      - api
    networks:
      - roll6-network

  caddy:
    image: caddy:2-alpine
    container_name: roll6-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web
    networks:
      - roll6-network
      - roll6-external

volumes:
  postgres_data:
  caddy_data:
  caddy_config:

networks:
  roll6-network:
    driver: bridge
  roll6-external:
    external: true
